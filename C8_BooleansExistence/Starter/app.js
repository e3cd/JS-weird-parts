////PRIMITIVE TYPE ==== a type of data that represents a single value, that is not an object.

//string, number, boolean, undefined, null, symbol

//null represents a lack of existence but you can set a variable to it. undefined should not have a value set

//symbol

////CONCEPTUAL ASIDE

/**
 * OPERATORS ==== a special function that is syntactically written differently. + - * > ; operators are functions. In the functions are Essentially are prewritten code that invoke these functions.
 */

/**
 * OPERATOR PRECEDENCE ==== WHICH OPERATOR FUNCTION GETS CALLED FIRST
 *
 * OPERATOR ASSOCIATIVITY ==== WHAT ORDER OPERATOR FUNCTIONS GET CALLED IN: LEFT TO RIGH OR RIGHT TO LEFT IF THEY HAVE THE SAME PRECEDENCE
 */

/**
 * COERCION ==== converting a value from one type to another
 *
 * var a = 1 + '2' --- 1 is coerced into string 1 by the js engine
 *
 */
