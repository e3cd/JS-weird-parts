var a = {};
var b = function() {};
var c = [];

/**
 * All objects in javascript are automatically have their proto property set to the built in object that has all the function methods.
 *
 * Object is the base prototype, bottom of the prototype chain.
 */
