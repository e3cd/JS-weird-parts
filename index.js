/**
 * CONCEPTUAL ASIDE syntax parser / lexical environments / execution contexts
 */

// // SYNTAX PARSERS:

// a program that reads our code and determines what it does and if its grammar is valid

// // LEXICAL ENVIRONMENT:

// Where somethings sits physically in the code you write, and what surrounds it.

// // EXECUTION CONTEXT:

// a wrapper to help manage the code that is running. The context can contain the code you write and things beyond

/**
 * Name/Value pairs and objects
 */

// // NAME/VALUE PAIR

// A name which maps to a unique value

//// OBJECT

// a collection of name value pairs
